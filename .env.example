# Pinata API credentials (get from https://app.pinata.cloud/developers/api-keys)
# Uncomment and fill in to enable Pinata pinning:
# PINATA_JWT=your-pinata-jwt-token-here

# Optional: custom IPFS gateway for fetching (default: localhost:8080)
# IPFS_GATEWAY=https://gateway.pinata.cloud
